<Window x:Class="ProductServiceWpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ProductServiceWpf"
        mc:Ignorable="d"
        Title="{Binding UiTitle}"
        Height="700"
        Width="1180"
        MinHeight="620"
        MinWidth="980"
        FontFamily="Segoe UI Variable Text"
        Background="#F3F6FB">

    <Window.Resources>
        <SolidColorBrush x:Key="BrushPage" Color="#F3F6FB"/>
        <SolidColorBrush x:Key="BrushCard" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="BrushPrimary" Color="#1677FF"/>
        <SolidColorBrush x:Key="BrushPrimaryHover" Color="#2A86FF"/>
        <SolidColorBrush x:Key="BrushBorder" Color="#D9E2F0"/>
        <SolidColorBrush x:Key="BrushText" Color="#1D2736"/>
        <SolidColorBrush x:Key="BrushMuted" Color="#5A677A"/>
        <SolidColorBrush x:Key="BrushStatus" Color="#EAF2FF"/>

        <Style x:Key="CardBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource BrushCard}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BrushBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="14"/>
        </Style>

        <Style x:Key="SectionTitleStyle" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Foreground" Value="{StaticResource BrushText}"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>

        <Style x:Key="FieldLabelStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource BrushMuted}"/>
            <Setter Property="Margin" Value="0,8,0,4"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="{StaticResource BrushPrimary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BrushPrimary}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,7"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="{StaticResource BrushPrimaryHover}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Root" Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="BorderBrush" Value="{StaticResource BrushBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Padding" Value="6"/>
            <Setter Property="BorderBrush" Value="{StaticResource BrushBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
        </Style>

        <Style TargetType="DataGrid">
            <Setter Property="BorderBrush" Value="{StaticResource BrushBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="RowBackground" Value="White"/>
            <Setter Property="AlternatingRowBackground" Value="#F9FBFF"/>
            <Setter Property="Background" Value="White"/>
        </Style>

        <Style TargetType="ListBox">
            <Setter Property="BorderBrush" Value="{StaticResource BrushBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Padding" Value="4"/>
        </Style>

        <Style x:Key="ThinScrollBar" TargetType="ScrollBar">
            <Style.Triggers>
                <Trigger Property="Orientation" Value="Vertical">
                    <Setter Property="Width" Value="10"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="16" Background="{StaticResource BrushPage}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0"
                       VerticalAlignment="Center"
                       FontSize="20"
                       FontWeight="SemiBold"
                       Foreground="{StaticResource BrushText}"
                       Text="{Binding UiTitle}"/>

            <Button Grid.Column="1"
                    Width="92"
                    Height="34"
                    Command="{Binding ToggleLanguageCommand}"
                    Content="{Binding UiLanguageButton}"/>
        </Grid>

        <TabControl Grid.Row="1" Background="Transparent" BorderThickness="0">
            <TabItem Header="{Binding UiTabProductsServices}">
                <Grid Margin="8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="360"/>
                        <ColumnDefinition Width="12"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Style="{StaticResource CardBorderStyle}">
                        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                            <ScrollViewer.Resources>
                                <Style TargetType="ScrollBar" BasedOn="{StaticResource ThinScrollBar}"/>
                            </ScrollViewer.Resources>
                            <StackPanel>
                                <TextBlock Style="{StaticResource SectionTitleStyle}" Text="{Binding UiActions}"/>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                                    <Button Width="140" Margin="0,0,8,0" Command="{Binding LoadFromXmlCommand}" Content="{Binding UiLoadXml}"/>
                                    <Button Width="140" Command="{Binding ClearCommand}" Content="{Binding UiClear}"/>
                                </StackPanel>

                                <Separator Margin="0,2,0,10"/>

                                <TextBlock Style="{StaticResource SectionTitleStyle}" Text="{Binding UiProduct}"/>
                                <TextBlock Style="{StaticResource FieldLabelStyle}" Text="{Binding UiName}"/>
                                <TextBox Text="{Binding ProdusNume, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}"/>
                                <TextBlock Style="{StaticResource FieldLabelStyle}" Text="{Binding UiInternalCode}"/>
                                <TextBox Text="{Binding ProdusCod, UpdateSourceTrigger=PropertyChanged}"/>
                                <TextBlock Style="{StaticResource FieldLabelStyle}" Text="{Binding UiProducer}"/>
                                <TextBox Text="{Binding ProdusProducator, UpdateSourceTrigger=PropertyChanged}"/>
                                <TextBlock Style="{StaticResource FieldLabelStyle}" Text="{Binding UiPrice}"/>
                                <TextBox Text="{Binding ProdusPret, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}"/>
                                <TextBlock Style="{StaticResource FieldLabelStyle}" Text="{Binding UiCategory}"/>
                                <TextBox Text="{Binding ProdusCategorie, UpdateSourceTrigger=PropertyChanged}"/>
                                <Button Margin="0,10,0,14" Command="{Binding AddProdusCommand}" Content="{Binding UiAddProduct}"/>

                                <Separator Margin="0,4,0,10"/>

                                <TextBlock Style="{StaticResource SectionTitleStyle}" Text="{Binding UiService}"/>
                                <TextBlock Style="{StaticResource FieldLabelStyle}" Text="{Binding UiName}"/>
                                <TextBox Text="{Binding ServiciuNume, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}"/>
                                <TextBlock Style="{StaticResource FieldLabelStyle}" Text="{Binding UiInternalCode}"/>
                                <TextBox Text="{Binding ServiciuCod, UpdateSourceTrigger=PropertyChanged}"/>
                                <TextBlock Style="{StaticResource FieldLabelStyle}" Text="{Binding UiPrice}"/>
                                <TextBox Text="{Binding ServiciuPret, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}"/>
                                <TextBlock Style="{StaticResource FieldLabelStyle}" Text="{Binding UiCategory}"/>
                                <TextBox Text="{Binding ServiciuCategorie, UpdateSourceTrigger=PropertyChanged}"/>
                                <Button Margin="0,10,0,0" Command="{Binding AddServiciuCommand}" Content="{Binding UiAddService}"/>
                            </StackPanel>
                        </ScrollViewer>
                    </Border>

                    <Border Grid.Column="2" Style="{StaticResource CardBorderStyle}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
                                <Button Width="170" Command="{Binding DeleteItemCommand}" Content="{Binding UiDeleteItem}"/>
                            </StackPanel>

                            <DataGrid Grid.Row="1"
                                      AutoGenerateColumns="True"
                                      ItemsSource="{Binding Items}"
                                      SelectedItem="{Binding SelectedItem}"
                                      IsReadOnly="False"/>
                        </Grid>
                    </Border>
                </Grid>
            </TabItem>

            <TabItem Header="{Binding UiTabPackages}">
                <Grid Margin="8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="360"/>
                        <ColumnDefinition Width="12"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="12"/>
                        <ColumnDefinition Width="320"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Style="{StaticResource CardBorderStyle}">
                        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                            <ScrollViewer.Resources>
                                <Style TargetType="ScrollBar" BasedOn="{StaticResource ThinScrollBar}"/>
                            </ScrollViewer.Resources>
                            <StackPanel>
                            <TextBlock Style="{StaticResource SectionTitleStyle}" Text="{Binding UiPackage}"/>
                            <TextBlock Style="{StaticResource FieldLabelStyle}" Text="{Binding UiName}"/>
                            <TextBox Text="{Binding PachetNume, UpdateSourceTrigger=PropertyChanged}"/>
                            <TextBlock Style="{StaticResource FieldLabelStyle}" Text="{Binding UiInternalCode}"/>
                            <TextBox Text="{Binding PachetCod, UpdateSourceTrigger=PropertyChanged}"/>
                            <TextBlock Style="{StaticResource FieldLabelStyle}" Text="{Binding UiCategory}"/>
                            <TextBox Text="{Binding PachetCategorie, UpdateSourceTrigger=PropertyChanged}"/>
                            <Button Margin="0,10,0,14" Command="{Binding CreatePachetCommand}" Content="{Binding UiCreatePackage}"/>

                            <Separator Margin="0,4,0,10"/>

                            <TextBlock Style="{StaticResource SectionTitleStyle}" Text="{Binding UiAddContent}"/>
                            <TextBlock Style="{StaticResource FieldLabelStyle}" Text="{Binding UiProduct}"/>
                            <ComboBox ItemsSource="{Binding Produse}" DisplayMemberPath="Nume" SelectedItem="{Binding SelectedProdus}"/>
                            <Button Margin="0,8,0,10" Command="{Binding AddProdusToPachetCommand}" Content="{Binding UiAddProductToPackage}"/>

                            <TextBlock Style="{StaticResource FieldLabelStyle}" Text="{Binding UiService}"/>
                            <ComboBox ItemsSource="{Binding Servicii}" DisplayMemberPath="Nume" SelectedItem="{Binding SelectedServiciu}"/>
                            <Button Margin="0,8,0,0" Command="{Binding AddServiciuToPachetCommand}" Content="{Binding UiAddServiceToPackage}"/>
                            </StackPanel>
                        </ScrollViewer>
                    </Border>

                    <Border Grid.Column="2" Style="{StaticResource CardBorderStyle}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
                                <Button Width="170" Command="{Binding DeletePachetCommand}" Content="{Binding UiDeletePackage}"/>
                            </StackPanel>

                            <DataGrid Grid.Row="1"
                                      AutoGenerateColumns="True"
                                      ItemsSource="{Binding Pachete}"
                                      SelectedItem="{Binding SelectedPachet}"
                                      IsReadOnly="False"/>
                        </Grid>
                    </Border>

                    <Border Grid.Column="4" Style="{StaticResource CardBorderStyle}">
                        <StackPanel>
                            <TextBlock Style="{StaticResource SectionTitleStyle}" Text="{Binding UiPackageContent}"/>
                            <ListBox ItemsSource="{Binding SelectedPachetContinut}"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </TabItem>
        </TabControl>

    </Grid>
</Window>
